<Window x:Class="AionMoneyCounter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AionMoneyCounter"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel, IsDesignTimeCreatable=True}"
        Title="Aion Money Counter"
        Height="750" Width="950" MinHeight="720" MinWidth="900"
        Background="#FF1E1E1E" Foreground="#E1E1E1" WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <Window.Resources>
        <local:StringListConverter x:Key="StringListConverter"/>
        <SolidColorBrush x:Key="PrimaryBackground" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="SecondaryBackground" Color="#FF2D2D2D"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FF252525"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FF3E3E42"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#FFE1E1E1"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#FFAAAAAA"/>
        <SolidColorBrush x:Key="AccentColor" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="AccentHoverColor" Color="#FF009AFF"/>
        <SolidColorBrush x:Key="AccentPressedColor" Color="#FF005C99"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#FF388E3C"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#FFD32F2F"/>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
        </Style>
        <Style x:Key="HeaderTextBlock" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        <Style x:Key="LabelTextBlock" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryColor}"/>
        </Style>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="4" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentPressedColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource SuccessColor}"/>
        </Style>
        <Style x:Key="ErrorButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource ErrorColor}"/>
        </Style>
        <Style x:Key="WarnButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#E65100"/>
        </Style>
        <Style x:Key="NeutralButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FF3E3E42"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" CornerRadius="4"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite"
                                        IsHitTestVisible="False"
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        Margin="8,4,28,4"
                                        VerticalAlignment="Center"/>
                                    <TextBlock Text="▾" HorizontalAlignment="Right" VerticalAlignment="Center"
                                               Margin="0,0,8,0" Foreground="{StaticResource TextSecondaryColor}"/>
                                </Grid>
                            </Border>
                            <ToggleButton IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Opacity="0" Focusable="False"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="{StaticResource SecondaryBackground}"
                                        BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                                        CornerRadius="4" MinWidth="{TemplateBinding ActualWidth}">
                                    <ItemsPresenter/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryColor}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" BorderThickness="0,0,0,2" BorderBrush="Transparent">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <TabControl Margin="10">
            <TabItem Header="Мониторинг">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,10,0">
                            <StackPanel>
                                <Button Style="{StaticResource SuccessButton}" Command="{Binding StartCommand}" Height="40" FontSize="16" FontWeight="Bold">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="▶" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White"/>
                                        <TextBlock Text="Старт" Foreground="White"/>
                                    </StackPanel>
                                </Button>
                                <Button Margin="0,10,0,0" Style="{StaticResource ErrorButton}" Command="{Binding StopCommand}" Height="40" FontSize="16" FontWeight="Bold">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="■" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White"/>
                                        <TextBlock Text="Стоп" Foreground="White"/>
                                    </StackPanel>
                                </Button>
                                <Button Margin="0,10,0,0" Style="{StaticResource WarnButton}" Command="{Binding PauseCommand}" Height="36" ToolTip="Приостановить/возобновить мониторинг без сброса счётчиков">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding PauseButtonText}" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White"/>
                                        <TextBlock Text="{Binding PauseButtonLabel}" Foreground="White"/>
                                    </StackPanel>
                                </Button>
                                <Button Margin="0,10,0,0" Style="{StaticResource NeutralButton}" Command="{Binding NewSessionCommand}" Height="36" ToolTip="Сбросить счётчики и начать считать с текущей позиции в логе">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="↺" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White"/>
                                        <TextBlock Text="Новая сессия" Foreground="White"/>
                                    </StackPanel>
                                </Button>
                                <Button Margin="0,10,0,0" Style="{StaticResource NeutralButton}" Command="{Binding RefreshPricesCommand}" Height="36" ToolTip="Загрузить актуальный прайс-лист с сервера">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⟳" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White"/>
                                        <TextBlock Text="Обновить цены" Foreground="White"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource CardBorder}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                                    <TextBlock Text="Статус: " FontSize="18" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding StatusText}" FontSize="18" FontWeight="Bold" Foreground="{Binding StatusColor}"/>
                                </StackPanel>

                                <UniformGrid Grid.Row="1" Columns="2" Margin="5,10,5,0">
                                    <StackPanel>
                                        <TextBlock Text="Предметов:" Style="{StaticResource LabelTextBlock}"/>
                                        <TextBlock Text="{Binding ItemCount}" FontSize="18"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="Игровая валюта (предметы):" Style="{StaticResource LabelTextBlock}"/>
                                        <TextBlock Text="{Binding TotalGameValue, StringFormat={}{0:N0}}" FontSize="18"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,10,0,0">
                                        <TextBlock Text="Кинара с мобов:" Style="{StaticResource LabelTextBlock}"/>
                                        <TextBlock Text="{Binding TotalKinahDropped, StringFormat={}{0:N0}}" FontSize="18" Foreground="#FFD700"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,10,0,0">
                                        <TextBlock Text="Цена FunPay (за 1кк):" Style="{StaticResource LabelTextBlock}"/>
                                        <TextBlock Text="{Binding FunPayPrice, StringFormat={}{0:F2} ₽}" FontSize="18"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,10,0,0">
                                        <TextBlock Text="Реальная стоимость:" Style="{StaticResource LabelTextBlock}"/>
                                        <TextBlock Text="{Binding RealValue, StringFormat={}{0:F2} ₽}" FontSize="18" FontWeight="Bold" Foreground="#66BB6A"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,10,0,0">
                                        <TextBlock Text="Время работы:" Style="{StaticResource LabelTextBlock}"/>
                                        <TextBlock Text="{Binding ElapsedTime}" FontSize="18" FontFamily="Consolas"/>
                                    </StackPanel>
                                </UniformGrid>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5,20,5,5">
                                    <TextBox x:Name="ManualKinahAmount" Width="180" ToolTip="Введите сумму" TextChanged="ManualKinahAmount_TextChanged"/>
                                    <Button Margin="10,0,5,0" Content="Добавить" Command="{Binding AddKinahCommand}" CommandParameter="{Binding Text, ElementName=ManualKinahAmount}"/>
                                    <Button Content="Отнять" Command="{Binding SubtractKinahCommand}" CommandParameter="{Binding Text, ElementName=ManualKinahAmount}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>

                    <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <DockPanel Grid.Row="0" Margin="5,3,5,6">
                                <TextBlock Text="Журнал событий" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryColor}"/>
                                <Button DockPanel.Dock="Right" Content="Очистить" Command="{Binding ClearLogCommand}" Padding="8,3" FontSize="12" HorizontalAlignment="Right"/>
                            </DockPanel>
                            <ListView x:Name="LogListView" Grid.Row="1"
                                      ItemsSource="{Binding LogMessages}"
                                      FontFamily="Consolas"
                                      FontSize="13"
                                      BorderThickness="0"
                                      HorizontalContentAlignment="Stretch"
                                      SelectionMode="Single"
                                      VirtualizingPanel.IsVirtualizing="True"
                                      VirtualizingPanel.VirtualizationMode="Standard"
                                      ScrollViewer.CanContentScroll="True">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Text}"
                                                   Foreground="{Binding TextColor}"
                                                   TextWrapping="Wrap"
                                                   Padding="5,3"/>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Focusable" Value="False"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Настройки" IsEnabled="{Binding IsNotRunning}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <StackPanel DataContext="{Binding Config}" Margin="0,0,15,10">
                        <Border Style="{StaticResource CardBorder}" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Style="{StaticResource HeaderTextBlock}" Text="Основные настройки"/>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Путь к лог-файлу:" Style="{StaticResource LabelTextBlock}"/>
                                    <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource SecondaryBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4" Padding="8" VerticalAlignment="Center" Margin="0,5">
                                        <TextBlock Text="{Binding LogFilePath}" TextTrimming="CharacterEllipsis" ToolTip="{Binding LogFilePath}"/>
                                    </Border>
                                    <Button Grid.Row="0" Grid.Column="2" Command="{Binding DataContext.BrowseLogFileCommand, RelativeSource={RelativeSource AncestorType=Window}}" Margin="10,5,0,5">
                                        <TextBlock Text="📂 Обзор..." Foreground="White"/>
                                    </Button>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Имя файла вывода:" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding OutputFilePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" ToolTip="Файл будет создан в папке с программой"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Кодировка лог-файла:" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding LogFileEncoding, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Символ реальной валюты:" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding CurrencySymbol, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource CardBorder}" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Style="{StaticResource HeaderTextBlock}" Text="Настройки FunPay"/>
                                <CheckBox IsChecked="{Binding FunPayIntegrationEnabled}" Content="Включить интеграцию с FunPay" Margin="0,5,0,10"/>
                                <Grid IsEnabled="{Binding FunPayIntegrationEnabled}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="URL API FunPay:" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FunPayApiUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Раса для отслеживания цены:" Style="{StaticResource LabelTextBlock}"/>
                                    <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,5" ItemsSource="{Binding DataContext.Factions, RelativeSource={RelativeSource AncestorType=Window}}" SelectedValue="{Binding FunPayFactionId}" SelectedValuePath="Id" DisplayMemberPath="Name"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Сервер (пусто = все):" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding FunPayServer, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" ToolTip="Название сервера для фильтра (оставьте пустым чтобы получить минимум по всем серверам)"/>
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Курс 1кк кинар к рублю:" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding InGameCurrencyRate, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Интервал обновления (сек):" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding FunPayUpdateIntervalSeconds, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Комиссия FunPay (%):" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding FunPayCommissionPercent, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Комиссия на вывод (%):" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding WithdrawalCommissionPercent, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource HeaderTextBlock}" Text="Настройки анализатора"/>
                                <CheckBox IsChecked="{Binding DebugMode}" Content="Режим отладки" Margin="0,5,0,5"/>
                                <CheckBox IsChecked="{Binding DirectLootMode}" Content="Прямой режим (без подтверждения)" Margin="0,0,0,10" ToolTip="Считать предметы сразу по префиксу строки, не дожидаясь фразы-подтверждения"/>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Интервал проверки лога (сек):" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CheckIntervalSeconds, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Окно поиска лута (сек):" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LootLookbackSeconds, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Фразы-подтверждения:" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ConfirmationMessages, Converter={StaticResource StringListConverter}, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" ToolTip="Разделять фразы точкой с запятой (;)"/>
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Префиксы строк с предметами:" Style="{StaticResource LabelTextBlock}"/>
                                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ItemLinePrefixes, Converter={StaticResource StringListConverter}, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" ToolTip="Разделять префиксы точкой с запятой (;)"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>